// TO add data into firestore

 data is array
 and dt is the element
 
  // const handleClick =  () =>{
  //   data.map( async (dt)=>{

  //     try {
  //       const docRef = await addDoc(collection(db, "movies"), dt);
  //       console.log("Document written with ID: ", docRef.id);
  //     } catch (e) {
  //       console.error("Error adding document: ", e);
  //     }

  //   })

  // }